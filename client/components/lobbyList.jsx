const React = require("react");

class LobbyList extends React.Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return (
      <select className="lobby-bar">
        <option>something</option>
      </select>
    );
  }
}

export default LobbyList;